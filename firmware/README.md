# Firmware

This folder contains the firmware for this. This is really really basic firmware right now, just *something*. Once the keyboard actually exists and this can be tested, there will be proper (probably ZMK-based) firmware.
